---
layout: default
---
<section>

  {{ content | safe }}

  <ul class="list">
  {% for post in collections.posts %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <p class="date">{{ post.date.toISOString() | readableDate }}</p>
    </li>
  {% endfor %}
  </ul>

</section>
